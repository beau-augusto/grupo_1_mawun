var ViDApp = angular.module('ViDApp');
ViDApp.controller('InfoModalInstanceCtrl', function ($uibModalInstance, dataService) {
  var $ctrl = this;
  $ctrl.title = 'Notes for QA';
  $ctrl.dataService = dataService;

  $ctrl.ok = function() {
    $uibModalInstance.close();
  }
});
